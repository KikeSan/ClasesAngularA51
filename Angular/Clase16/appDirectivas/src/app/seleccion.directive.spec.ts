import { SeleccionDirective } from './seleccion.directive';

describe('SeleccionDirective', () => {
  it('should create an instance', () => {
    const directive = new SeleccionDirective();
    expect(directive).toBeTruthy();
  });
});
